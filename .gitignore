node_modules/
.next/
.env.local
.env*.local
*.db

.vercel
.env*.local

# File server + Puppeteer server secrets — both .env files hold
# FILE_SERVER_KEY. Must never be committed. .env.example in each folder IS
# committed and shows the expected keys with dummy values.
scripts/file-server/.env
scripts/puppeteer-server/.env

# Microsoft Office lock files — created when a .docx/.xlsx is open in Word/Excel
# on Windows. They're transient and contain the editing user's name.
~$*

# Claude Code local scratch — never commit
.claude/worktrees/
.claude/cache/
.claude/logs/
.claude/sessions/

# Environment files
.env
.env.local
.env.production

# Supabase local
supabase/.branches/
supabase/.temp/

# pg_dump output files from one-off migrations (contain prod data)
scripts/migration_helpers/old_data_dump*.sql
scripts/migration_helpers/*.dump

# Claude Code local scratch � never commit
.claude/worktrees/
.claude/cache/
.claude/logs/
.claude/sessions/

# Environment files
.env
.env.local
.env.production

# Supabase local
supabase/.branches/
supabase/.temp/
